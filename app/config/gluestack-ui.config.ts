
import { createConfig } from "@gluestack-ui/themed"

export const config = createConfig({
  light: {
    colors: {
      primary: '#000000',
      background: '#FFFFFF',
    },
  },
  dark: {
    colors: {
      primary: '#FFFFFF', 
      background: '#000000',
    },
  },
})
